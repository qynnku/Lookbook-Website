# Lookbook Frontend

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start dev server:
   ```sh
   npm run dev
   ```

- App runs at http://localhost:5173
- Make sure backend is running at http://localhost:4000
- To use API, you must login and set the JWT token in requests (see backend README)
